---
// src/components/Tips.astro
const { dict } = Astro.props;
---

<section class="max-w-6xl mx-auto px-6 text-center" >
     &nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2" >{dict.tips.title}</h2>

  <p set:html={dict.tips.intro}></p>
&nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.tips.tip1Title}</h2>
  <p set:html={dict.tips.tip1}></p>
&nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.tips.tip2Title}</h2>
  <p set:html={dict.tips.tip2}></p>
&nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.tips.tip3Title}</h2>
  <p set:html={dict.tips.tip3}></p>
&nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.tips.tip4Title}</h2>
  <p set:html={dict.tips.tip4}></p>
&nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.tips.tip5Title}</h2>
  <p set:html={dict.tips.tip5}></p>

  <p set:html={dict.tips.conclusion}></p>
</section>
