---
// src/components/Platform.astro
const { dict } = Astro.props;
---

<section class="max-w-6xl mx-auto px-6 text-center" >
 &nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.platform.title}</h2>

  <p>{dict.platform.subtitle}</p>

  <p set:html={dict.platform.content}></p>
</section>
