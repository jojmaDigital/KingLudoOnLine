---
// src/components/Features.astro
const { dict } = Astro.props;
---

<section class="max-w-6xl mx-auto px-6 text-center" >
    &nbsp;
  <h2 class="text-3xl font-extrabold text-center mb-12 flex items-center justify-center gap-2">{dict.features.title}</h2>
  <p set:html={dict.features.content}></p>
</section>

