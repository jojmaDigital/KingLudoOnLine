---
import Layout from "../../layouts/Layout.astro";
import { getLangStaticPaths, resolveLang } from "../../lib/i18n";
import HtmlContent from "../../components/HtmlContent.astro";

import Footer from "../../components/Footer.astro";
export async function getStaticPaths() {
  return getLangStaticPaths();
}

const locale = resolveLang(Astro.params.lang);
const dict = await import(`../../locales/${locale}.json`).then((m) => m.default);
---

<Layout lang={locale} title={dict.termsTitle}>
  <!-- Hero Section -->
  <section class="relative bg-yellow-400 text-white py-20 overflow-hidden">
    <div class="absolute inset-0 bg-[url('/bg-pattern.svg')] opacity-10"></div>
    <div class="relative max-w-5xl mx-auto px-6 text-center">
      <h1 class="text-4xl md:text-6xl font-extrabold mb-6 tracking-tight">
        {dict.termsTitle}
      </h1>
      <p class="text-lg md:text-xl opacity-90 max-w-3xl mx-auto leading-relaxed">
        {dict.termsSubtitle || "Please read our terms carefully to understand your rights and responsibilities while using our services."}
      </p>
    </div>
  </section>

  <!-- Main Content Section -->
  <section class="relative max-w-6xl mx-auto px-6 py-24">
    <!-- Decorative background -->
    <div class="absolute inset-0 bg-gradient-to-r from-gray-200 via-gray-100 to-gray-200 opacity-60 rounded-[3rem] blur-3xl"></div>

    <!-- Content Wrapper -->
    <div class="relative bg-white/90 backdrop-blur-sm border border-gray-100 rounded-3xl shadow-2xl p-10 md:p-16 hover:shadow-[0_0_40px_rgba(0,0,0,0.1)] transition duration-500">
      <div class="prose prose-lg prose-gray mx-auto leading-relaxed">
        <HtmlContent slug="terms" locale={locale} fallbackText="Terms of service not available." />
      </div>
    </div>
  </section>

  <!-- Call to Action -->
  <section class=" text-center text-white">
    <div class="max-w-4xl mx-auto px-6">
      <h2 class="text-3xl md:text-4xl font-extrabold mb-6">
        {dict.termsCTA || "By playing, you agree to our terms"}
      </h2>
      <p class="text-lg opacity-90 mb-8">
        {dict.termsCTAText || "We are committed to providing a fair, transparent, and enjoyable experience for all our players."}
      </p>
      <a href="/{locale}/play" class="inline-block bg-white text-gray-800 font-semibold px-8 py-4 rounded-xl shadow-md hover:bg-gray-100 transition">
        {dict.termsCTAButton || "Play King ludo Now"}
      </a>
    </div>
  </section>
</Layout>
    <footer>
    <Footer siteName="Ludo King Online" locale={locale} />
